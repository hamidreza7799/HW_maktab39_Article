package repositories.metadata;

import base.repositories.BaseRepository;
import domains.metadata.TableColumnMetadata;

public interface TableColumnMetadataRepository extends BaseRepository<TableColumnMetadata,Integer> {
}
